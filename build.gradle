apply plugin: 'java'

jar {
  dependsOn 'createArchives'
  from(fileTree("$buildDir/archives")) {  
      into "META-INF/templates" 
  }
}

task createArchives(type: Tar) {
  archiveFileName = "java.tar"
  destinationDirectory = file("$buildDir/archives")
  from "java"
}